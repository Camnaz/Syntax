// Integration tests are skipped for binary crates
// Run these manually with: cargo test --test llm_integration -- --ignored
// Note: Requires ANTHROPIC_API_KEY and GEMINI_API_KEY in .env

// These tests verify real API calls work correctly
// They are ignored by default to avoid consuming API credits during normal test runs
